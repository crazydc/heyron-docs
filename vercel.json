{
  "buildCommand": "npm install && npx prisma generate && cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "installCommand": "npm install && npx prisma generate && cd client && npm install"
}
