{
  "buildCommand": "npm install && cd client && npm install --include=dev && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "installCommand": "npm install && cd client && npm install --include=dev",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/sync-user"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
